version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: mx-tools-redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
